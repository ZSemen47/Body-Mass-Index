public class BmiService {
    public double calculate (double height, double weight) {
        double heightHeight = height * height;
        double bmi = weight / heightHeight;
        return bmi;
    }
}